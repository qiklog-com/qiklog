# QikLog Console — Desktop

Electron BrowserWindow shell for macOS, Windows, and Linux.

```bash
npm install
npm start

# Local dashboard
QIKLOG_APP_URL=http://localhost:5081 npm start

# Packaged builds (optional)
npm run dist
```

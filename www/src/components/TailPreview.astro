---
// Demo line option 3 (founder vote). Typewriter steps/max-width = message.length (15).
const message = 'JWT expired 401';
---
<div class="preview" role="img" aria-label={`Example log line: 14:02:11 INFO demo ${message}`}>
  <p class="chrome">demo</p>
  <p class="line">
    <span class="meta">14:02:11 INFO demo</span>
    <span class="msg">{message}</span><span class="cursor" aria-hidden="true"></span>
  </p>
</div>

<style>
  .preview {
    width: 100%;
    min-height: 11rem;
    padding: 1.2rem 1.35rem 1.4rem;
    background: var(--ql-ink);
    color: var(--ql-paper);
    font-family: var(--ql-font-mono);
    font-size: 1.05rem;
    line-height: 1.55;
    border-radius: var(--ql-radius-sm);
    box-shadow: var(--ql-shadow);
  }

  .chrome {
    margin: 0 0 1rem;
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ql-ink-caption);
  }

  .line {
    margin: 0;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }

  .meta {
    color: var(--ql-hairline);
    margin-right: 0.5ch;
  }

  .msg {
    display: inline-block;
    overflow: hidden;
    vertical-align: bottom;
    max-width: 0;
    white-space: nowrap;
  }

  :global(#scene-1.is-in) .msg,
  :global(#scene-1.no-motion) .msg {
    animation: type 2.4s steps(15, end) forwards;
  }

  .cursor {
    display: inline-block;
    width: 0.5ch;
    height: 0.95em;
    margin-left: 1px;
    background: var(--ql-rust);
    vertical-align: -0.1em;
    animation: blink 1.1s steps(1, end) infinite;
  }

  @keyframes type {
    from {
      max-width: 0;
    }
    to {
      max-width: 15ch;
    }
  }

  @keyframes blink {
    0%,
    49% {
      opacity: 1;
    }
    50%,
    100% {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :global(#scene-1.is-in) .msg,
    :global(#scene-1.no-motion) .msg,
    .msg {
      animation: none;
      max-width: 15ch;
    }

    .cursor {
      animation: none;
      opacity: 1;
    }
  }
</style>
